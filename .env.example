# Copy this file to .env.local and fill in real values there.
# .env.local is gitignored (see .gitignore's "*.local" rule) — never commit real keys.

# Gemini API key from Google AI Studio, used only by api/maithili.js
# (a server-side Vercel function). Never referenced from client code.
GEMINI_API_KEY=
